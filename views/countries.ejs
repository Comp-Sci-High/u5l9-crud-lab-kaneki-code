<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8">
      <title>Cool Country Cards</title>
      <link rel="stylesheet" href="/style.css">
   </head>
   <body>
      <h1>🌍 Featured Countries</h1>
      <div class="card-container">

      <!-- Loop through all countries using a for loop -->
<%for(letx=0;x<data.length;x++%>{%>
         <div class="card">
             <!-- Use the flagURL key to set the image url -->
            <img src="<%= data[x].flagURL%>" alt="USA flag">
            <div class="info">
               <!-- Use the name key to set the h2 tag -->
               <h2><%= data[x].country%></h2>

              <!-- Use the population key to set the span tag -->
               <p>🧑‍🤝‍🧑 Population: <span><%= data[x].flagURL%></span></p>

              <!-- Use the officialLanguage key to set the span tag -->
               <p>🗣️ Language: <span><%= data[x].officialLanguage></span></p>

              <!-- Use the hasNuclearWeapons key to set the span tag -->
               <p>☢️ Nuclear Weapons: <span><%= data[x].hasNuclearWeapons</span></p>
            </div>
         </div>

      <!-- End loop here -->
       <%}%>
      </div>
   </body>
</html>
